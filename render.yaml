services:
  - type: web
    name: personal-task-manager-mvp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: RENDER
        value: "true"
    disk:
      name: task-data-volume
      mountPath: /var/data
      sizeGB: 1
